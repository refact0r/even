1: 
First I want you to read the documentation, Im trying to build a note/document app for the even G2's, here is my proposed app layout, I want the user to in the web view (phone side app) have a form to input an api key, you should have a link to openrouter.ai under the form so the user can get a apikey, additionally I want a connect to glassese button for debug, and a collapsable debug log view at the bottom, additionally under the apikey form I want there to be a file upload button for uploading SPECIFICALLY .txt files, and another button for image upload, the image button should send a image option should make an api call with some data(image/pdf) to an openrouter.ai endpoint (this should also encompass pdfs), we call our AI endpoint with the specific datastream option, (the request should be handeled in client.ts and the pipeline should be defined in the data-stream), we should also support for testing a direct file read button for locally stored files (.txt) and they should be loaded in and sent to the glasses, NOW for the UI and connection to the Glasses we should first start out in a recent file list with time stamps to the left and a 11 charector title to the right side of the list item, each item should be selectable and navigate the user to the next UI screen, there are 3 screens 0 (recent-view) 1 (logform-view) and 2 (which is full-text view), when we select a file we should first parse it (All files that are stored after the api returns or uploaded by the user should be formatted as so in json: each document holds the following json strcuture: there should be a type and title (Used for rendering in recent-view and full-view) at the very top, the type should either be 0 or 1 and this decides the next view, finally for files that are type 1 we need the format to go like this {TOPIC: "" , content: ""} this is used to render multiple "Chapters" in long-form-view (Type 1 always reroutes the user to long form view from the recent-view) from here (long-form-view) we need 2 containers, one listing all the topics and the other displaying the corresponding content, we should first have the topic list container be eventCapture so we can select topics, when we select (are at a specific index in the topics-list) we should render that topics content to a adjacent text container and update on scroll, so if I go from list item 1 to list item 2, the text container should show me the content of the first then the second, now when we select said item, we should then switch eventCapture from the list container to the text container we can now scroll the contents of the topic, For the final view it is shared between type 0 and type 1 documents, it is full-view, to get here we can either click the content text container we are currently in and this will send use to full view, all full view is is a textcontainer with maximum dimensions, this should update the screen but still have the same content as the previous view just in a larger container. Now the other way to acess this view if a file in recent is type 0 (short form content) then from reecent-view we should emmediately go to full-view with the contents of the file. for type 0 the json schema should go type, title (for rendering in recent-view) and content which there will only be one of, unlike type 1 where it can have as many chapters with there own content. to navigate backwards from the full text view we need to consider where we came from (all backwards navigations are double-click and forward are single), if we are type 0 we go straight back to the recent-view, if we are type 1, we go back to the long-form-view, and from the long-form-view we should go back to the recent view. this is the basic functionality for already stored files, next we will build out a connection to an AI for image to text (from-image) and and link to text (from-link) for now we will focus on from file so we can test out the UI, files to complete: from-file.ts (simple file load), main.ts, index.html(webView  entrypoint), controller.ts (handles scroll, click and double click gestures), all files in UI. additionally in the webView add colapsable debug log for troublshooting, and setup the vite.config.ts 

2: 
read the documentation and we will work on the, openrouter.ai config we should make calls with context of a STRICT ouput format, outlined in prompt.txt, add prompts for ./client/prompts for short view and long view context, in the webView dont change anything but add a link to openrouter.ai so we can get a api key in the same webView to put into the form, after this we can use the fromImage and from URL pipeline, for each we should do the following, take a paramater (short form or long form in the webview) then upload a file, then we make a call to our api with that file or URL, for url's ask the LLM to read the site and provide a Summary in either long form or short form style (as defined in prompt.txt) we should then get back a .json formatted reponse that we store in local storage in the webView, and this loads it into our recent view on refresh, then we should sanatize this file to make sure it follows the structure and from there treat it with regards to its type. files to edit: from-file, from-link, client (for api calls and formatting with prompts) , all files in prompt folder, and index.html to include the link to the api site, can you reiterate what I need and propose a way we can implement file sending with specific prompt scaffolding to openrouter.ai ? after you make we settle on a archetecture we can make a road map and execute,  